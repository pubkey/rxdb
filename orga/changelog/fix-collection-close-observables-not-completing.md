- FIX `RxCollection.close()` not completing the collection-scoped observables (`collection.$`, `insert$`, `update$`, `remove$`, `checkpoint$` and query `.$` observables), causing subscribers to stay attached to the database event stream after the collection was closed. Re-creating a collection with the same name on the same database could therefore leak change events into observers of the previously closed collection.
