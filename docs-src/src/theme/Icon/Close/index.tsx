import React, { type ReactNode } from 'react';
import type { Props } from '@theme/Icon/Close';
import { IconClose as IconCloseRx } from '@site/src/components/icons/close';

export default function IconClose({
  // width = 21,
  // height = 21,
  // color = 'currentColor',
  // strokeWidth = 1.2,
  // className,
  // ...restProps
}: Props): ReactNode {
  return <IconCloseRx />
}
