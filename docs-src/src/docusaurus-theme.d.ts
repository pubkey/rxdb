/**
 * Ambient type declarations for the swizzled components in src/theme.
 * Docusaurus ships the `@theme/*` module declarations inside the theme
 * packages, they have to be referenced explicitly so that `tsc` can resolve
 * imports like `import Layout from '@theme/DocItem/Layout';`.
 * @link https://docusaurus.io/docs/typescript-support
 */

/// <reference types="@docusaurus/module-type-aliases" />
/// <reference types="@docusaurus/theme-classic" />
/// <reference types="@docusaurus/theme-search-algolia" />
/// <reference types="@docusaurus/plugin-content-docs" />
/// <reference types="@docusaurus/plugin-content-blog" />
/// <reference types="@docusaurus/plugin-content-pages" />
