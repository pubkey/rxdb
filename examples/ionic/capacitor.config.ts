import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'info.rxdb.example.ionic',
    appName: 'RxDB Ionic Heroes',
    webDir: 'dist/ionic/browser'
};

export default config;
